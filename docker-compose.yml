services:
  pitch-toss-ai:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./assets/generated:/app/assets/generated
    restart: unless-stopped
